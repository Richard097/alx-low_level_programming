#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	char concat;

	char *str_concat(char *s1, char *s2);

	printf("%d\n", concat);
	free(void *concat);
	return (0);
}
